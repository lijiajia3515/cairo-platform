

 <UploadFile @change="onChangeFiles" :limit="10" :fileList="editFileList"></UploadFile>

import UploadFile from '@/components/uploadFile';

 const [fileList, setFileList] = useState([]);

const [editFileList, setEditFileList] = useState([
  {
    name: 'demo-image-1.png', url: 'https://tdesign.gtimg.com/demo/demo-image-1.png'
  }
]); // 编辑 过去显示

const onChangeFiles = (files) => {
  setFileList(files)
}

// 编辑的时候直接设置  editFileList  
// 参数 url  
// 参数 name: row.AvatarUrl.split('/')[row.AvatarUrl.split('/').length - 1]